<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Contact Us</title>

  <style>
    body{margin:0;font-family:Arial;background:#f1f3f6;}
    .header{background:#2874f0;color:#fff;padding:14px 30px;font-size:20px;}
    .contact-section{padding:70px 15px;display:flex;justify-content:center;}
    .contact-card{
      background:#fff;
      max-width:420px;
      width:100%;
      padding:35px 30px;
      border-radius:12px;
      box-shadow:0 15px 35px rgba(0,0,0,0.2);
      text-align:center;
    }
    .contact-card h2{color:#2874f0;margin-bottom:10px;}
    .contact-info p{
      background:#f9f9f9;
      padding:12px;
      border-radius:8px;
      margin-bottom:12px;
      font-size:14px;
    }
    .contact-info span{font-weight:bold;color:#ff7a18;}
  </style>
</head>
<body>

<div class="header">AutoKart – Contact Us</div>

<section class="contact-section">
  <div class="contact-card">
    <h2>📞 Contact Us</h2>
<% if (!contact_us || contact_us.length === 0) { %>
  <p>No Contact Details Available</p>
<% } else { %>
  <% contact_us.forEach((c) => { %>
    <div class="contact-info">
      <p><span>📍 Address</span><br><%= c.address %></p>
      <p><span>📞 Phone</span><br><%= c.phone %></p>
      <p><span>📧 Email</span><br><%= c.email %></p>
<p><span>🕒 Time</span><br><%= c.working_hours %></p>
    </div>
  <% }) %>
<% } %>


  </div>
</section>

</body>
</html>
