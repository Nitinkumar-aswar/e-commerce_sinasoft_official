<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>AutoKart – Interior Decoration Accessories</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body { background-color: #f4f6f9; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    .navbar { background-color: #1e6bff; padding: 12px 0; }
    .navbar-brand { color: #fff !important; font-size: 24px; font-weight: 700; }
    .page-title { margin: 40px 0 30px; font-size: 28px; font-weight: 700; text-align: center; color: #1e6bff; }
    .product-card { background: #fff; border-radius: 15px; box-shadow: 0 10px 20px rgba(0,0,0,0.12); overflow: hidden; transition: 0.3s; height: 100%; }
    .product-card:hover { transform: translateY(-8px); }
    .img-box { width: 100%; height: 220px; background: #f1f1f1; display: flex; align-items: center; justify-content: center; overflow: hidden; }
    .img-box img { width: 100%; height: 100%; object-fit: cover; }
    .card-body h5 { font-weight: 700; font-size: 18px; }
    .card-body p { font-size: 14px; color: #555; }
    .price { font-size: 16px; font-weight: 700; color: #1e6bff; }
    .btn-buy { background: linear-gradient(90deg, #1e6bff, #4dabf7); color: #fff; border: none; border-radius: 8px; width: 100%; }
  </style>
</head>
<body>

<nav class="navbar">
  <div class="container">
    <a class="navbar-brand" href="/">AutoKart</a>
  </div>
</nav>

<div class="container">
  <h2 class="page-title">Interior Decoration Accessories</h2>

  <div class="row g-4">
    <% if (interior_decoration.length > 0) { %>
      <% interior_decoration.forEach(item => { %>

        <div class="col-sm-6 col-md-4 col-lg-3 d-flex">
          <div class="product-card w-100">

            <div class="img-box">
              <% if (item.interior_image) { %>
                <img src="/upload/<%= item.interior_image %>" alt="<%= item.interior_title %>">
              <% } else { %>
                <span>No Image</span>
              <% } %>
            </div>

            <div class="card-body d-flex flex-column justify-content-between">
              <div>
                <h5><%= item.interior_title %></h5>
                <p><%= item.interior_heading %></p>
                <p class="price">₹ <%= item.price %></p>
              </div>

              <button class="btn btn-buy">Buy Now</button>
            </div>

          </div>
        </div>

      <% }) %>
    <% } else { %>
      <div class="col-12 text-center text-danger">
        No Interior Decoration Products Available
      </div>
    <% } %>
  </div>
</div>

</body>
</html>
