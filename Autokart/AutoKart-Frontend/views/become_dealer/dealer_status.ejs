<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dealer Request Status</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    *{
      margin:0;
      padding:0;
      box-sizing:border-box;
      font-family:'Inter',sans-serif;
    }

    body{
      background:#f4f7fb;
      color:#1f2937;
    }

    /* HEADER */
    .header{
      background:#fff;
      padding:18px 40px;
      border-bottom:1px solid #e5e7eb;
      display:flex;
      align-items:center;
    }

    .header h1{
      font-size:20px;
      font-weight:700;
      color:#111827;
    }

    /* PAGE WRAPPER */
    .container{
      max-width:1200px;
      margin:40px auto;
      padding:0 20px;
    }

    /* PAGE TITLE */
    .page-title{
      margin-bottom:24px;
    }

    .page-title h2{
      font-size:24px;
      font-weight:700;
      margin-bottom:6px;
    }

    .page-title p{
      color:#6b7280;
      font-size:14px;
    }

    /* STATUS PANEL */
    .status-panel{
      background:#fff;
      border-radius:14px;
      padding:32px;
      box-shadow:0 10px 28px rgba(0,0,0,0.06);
    }

    .dealer-info{
      display:flex;
      justify-content:space-between;
      align-items:center;
      margin-bottom:28px;
      border-bottom:1px solid #e5e7eb;
      padding-bottom:18px;
    }

    .dealer-info .name{
      font-size:18px;
      font-weight:600;
    }

    .dealer-info .label{
      color:#6b7280;
      font-size:13px;
      margin-bottom:4px;
    }

    /* STATUS BOX */
    .status-box{
      padding:18px 20px;
      border-radius:12px;
      font-size:16px;
      font-weight:500;
      display:flex;
      align-items:center;
      gap:14px;
      margin-bottom:26px;
    }

    .status-box.success{
      background:#ecfdf5;
      color:#047857;
      border:1px solid #6ee7b7;
    }

    .status-box.error{
      background:#fef2f2;
      color:#b91c1c;
      border:1px solid #fecaca;
    }

    .status-box.warning{
      background:#fffbeb;
      color:#92400e;
      border:1px solid #fde68a;
    }

    .status-icon{
      font-size:22px;
    }

    /* MESSAGE */
    .message{
      font-size:15px;
      color:#374151;
      line-height:1.7;
      margin-bottom:30px;
    }

    /* FOOTER ACTION */
    .actions{
      display:flex;
      justify-content:flex-end;
    }

    .btn{
      background:#2563eb;
      color:#fff;
      padding:12px 26px;
      border-radius:8px;
      text-decoration:none;
      font-size:14px;
      font-weight:600;
      transition:.3s;
    }

    .btn:hover{
      background:#1d4ed8;
    }

    /* RESPONSIVE */
    @media(max-width:768px){
      .dealer-info{
        flex-direction:column;
        align-items:flex-start;
        gap:10px;
      }
      .actions{
        justify-content:center;
      }
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <div class="header">
    <h1>Dealer Portal</h1>
  </div>

  <!-- CONTENT -->
  <div class="container">

    <div class="page-title">
      <h2>Dealer Request Status</h2>
    </div>

    <div class="status-panel">

      <!-- DEALER INFO -->
      <div class="dealer-info">
        <div>
          <div class="label">Dealer Name</div>
          <div class="name"><%= name %></div>
        </div>
      </div>

      <!-- STATUS -->
      <% if (type === 'success') { %>
        <div class="status-box success">
          <span class="status-icon">✅</span>
          <span><%= message %></span>
        </div>
      <% } else if (type === 'error') { %>
        <div class="status-box error">
          <span class="status-icon">❌</span>
          <span><%= message %></span>
        </div>
      <% } else { %>
        <div class="status-box warning">
          <span class="status-icon">⏳</span>
          <span><%= message %></span>
        </div>
      <% } %>


      <% if (type === 'error' && reason) { %>
  <div class="reject-reason">
    <strong>Reason:</strong> <%= reason %>
  </div>
<% } %>


      <div class="message">
        If you have any questions regarding your request, our team will contact you shortly.
        You may also revisit the registration page for further updates.
      </div>

      <div class="actions">
        <a href="/admin/become_dealer" class="btn">← Back to Dealer Page</a>
      </div>

    </div>

  </div>

</body>
</html>