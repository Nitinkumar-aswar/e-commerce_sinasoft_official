<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Profile | AutoKart</title>
  <link rel="stylesheet" href="styles.css">
  <!-- Font Awesome CDN -->
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="user_cdn/css/my_profile.css">
  <style>



    * { margin: 0; padding: 0; box-sizing: border-box; font-family: Arial, sans-serif; }
    body { background: #f1f3f6; }

    /* TOPBAR */
    .topbar { background: #010a16; padding: 10px 20px; display: flex; align-items: center; color: #fff; }
    .topbar .logo { font-size: 20px; font-weight: bold; margin-right: 20px; }
    .topbar .logo span { font-size: 12px; color: #ffe500; }
    .topbar input { flex: 1; padding: 8px; border: none; border-radius: 2px; }
    .nav-right { display: flex; gap: 20px; margin-left: 20px; font-size: 14px; }

    /* LAYOUT */
    .container { display: flex; width: 95%; margin: 20px auto; }

    /* SIDEBAR */
    .sidebar { width: 25%; }
    .user-box { background: #fff; padding: 15px; display: flex; align-items: center; gap: 10px; margin-bottom: 15px; }
    .user-box img { width: 50px; }
    .menu { background: #fff; padding: 10px 0; }
    .menu-title { padding: 12px 20px; font-weight: bold; color: #555; }
    .menu a { display: block; padding: 10px 40px; color: #333; text-decoration: none; font-size: 14px; }
    .menu a.active { background: #f1f3f6; color: #2874f0; font-weight: bold; }
    .green { color: green; float: right; }

    /* CONTENT */
    .content { width: 75%; margin-left: 20px; }
    .card { background: #fff; padding: 20px; margin-bottom: 15px; }
    .card h3 { font-size: 18px; margin-bottom: 15px; }
    .card h3 span { color: #2874f0; font-size: 14px; cursor: pointer; margin-left: 10px; }
    .row { display: flex; gap: 15px; }
    .row input { flex: 1; padding: 10px; border: 1px solid #ccc; }
    .gender { margin-top: 15px; }
    .gender p { margin-bottom: 5px; font-weight: bold; }
    .gender label { margin-right: 15px; }
    .logo img {
  height: 60px;
  object-fit: contain;
}


/* ================== RESPONSIVE DESIGN ================== */

/* ---------- LARGE SCREENS (Desktop ≥1200px) ---------- */
@media (min-width: 1200px) {
  .container {
    width: 95%;
  }
}

/* ---------- LAPTOP / SMALL DESKTOP (992px – 1199px) ---------- */
@media (max-width: 1199px) {
  .sidebar {
    width: 30%;
  }
  .content {
    width: 70%;
  }
}

/* ---------- TABLET (768px – 991px) ---------- */
@media (max-width: 991px) {
  .topbar {
    flex-wrap: wrap;
    gap: 10px;
  }

  .topbar input {
    width: 100%;
    margin-top: 8px;
  }

  .container {
    flex-direction: column;
  }

  .sidebar,
  .content {
    width: 100%;
    margin-left: 0;
  }

  .row {
    flex-direction: column;
  }

  .row input {
    width: 100%;
  }
}

/* ---------- MOBILE (≤767px) ---------- */
@media (max-width: 767px) {

  /* TOPBAR */
  .topbar {
    flex-direction: column;
    align-items: flex-start;
  }

  .nav-right {
    width: 100%;
    justify-content: space-between;
    font-size: 13px;
    margin-top: 8px;
  }

  /* USER BOX */
  .user-box {
    flex-direction: column;
    align-items: flex-start;
    text-align: left;
  }

  .user-box img {
    width: 45px;
  }

  /* SIDEBAR MENU */
  .menu a {
    padding: 10px 20px;
  }

  /* CONTENT */
  .card {
    padding: 15px;
  }

  .card h3 {
    font-size: 16px;
  }

  input {
    font-size: 14px;
  }
}

/* ---------- VERY SMALL MOBILE (≤480px) ---------- */
@media (max-width: 480px) {
  .topbar input {
    padding: 6px;
    font-size: 13px;
  }

  .nav-right span {
    font-size: 12px;
  }

  .gender label {
    display: block;
    margin-bottom: 5px;
  }
}

  </style>
</head>
<body>

<!-- TOP NAVBAR -->
<header class="topbar">
  <div class="logo">
  <a href="/">
    <img src="/uploads/autokart_logo.jpeg" alt="Autokart Logo" style="height: 60px; object-fit: contain;">
  </a>
</div>
  <input type="text" placeholder="Search for products, brands and more">
  <div class="nav-right">
    <span class="user"><i class="fa-solid fa-user"></i>
<%= user.user_user_name %> </span>
    <span>Become a Seller</span>
    <span>More ⌄</span>
    <span class="cart"><i class="bi bi-cart"></i> Cart</span>
  </div>
</header>

<!-- MAIN CONTAINER -->
<div class="container">

  <!-- LEFT SIDEBAR -->
  <aside class="sidebar">
    <div class="user-box">
      <img src="https://cdn-icons-png.flaticon.com/512/149/149071.png">
      <div>
        <p>Hello,</p>
        <h4><%= user.user_first_name %> <%= user.user_last_name %></h4>
        <p>Email: <%= user.user_email %></p>
      </div>
    </div>
    <div class="menu">
      <div class="menu-title"><i class="bi bi-box"></i> MY ORDERS</div>

      <div class="menu-title"><i class="bi bi-person"></i> ACCOUNT SETTINGS</div>
      <a class="active">Profile Information</a>
      <a>Manage Addresses</a>
      <a>PAN Card Information</a>

      <div class="menu-title"><i class="bi bi-credit-card"></i> PAYMENTS</div>
      <a>Gift Cards <span class="green">₹0</span></a>
      <a>Saved UPI</a>
      <a>Saved Cards</a>
    </div>
  </aside>

  <!-- RIGHT CONTENT -->
  <main class="content">

    <section class="card">
      <h3>Personal Information <span>Edit</span></h3>
      <div class="row">
        <input type="text" value="<%= user.user_first_name %>">
        <input type="text" value="<%= user.user_last_name %>">
      </div>

      <div class="gender">
        <p>Your Gender</p>
        <label><input type="radio" name="gender" <% if(user.gender === 'Male'){ %> checked <% } %> > Male</label>
        <label><input type="radio" name="gender" <% if(user.gender === 'Female'){ %> checked <% } %> > Female</label>
      </div>
    </section>

    <section class="card">
      <h3>Email Address <span>Edit</span></h3>
      <input type="email" value="<%= user.user_email %>">
    </section>

    <section class="card">
      <h3>Mobile Number <span>Edit</span></h3>
      <input type="text" value="<%= user.user_mobile %>">
    </section>

  </main>
</div>



<div class="nav-right">
  
  
  <a href="/customer_login" style="margin-left: 15px; color: black; text-decoration: none;">Logout</a>
</div>



</body>
</html>