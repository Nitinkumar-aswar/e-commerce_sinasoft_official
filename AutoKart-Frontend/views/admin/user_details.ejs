

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>User List</title>

  <!-- ✅ FONT AWESOME (Eye Icons) -->
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
  />
<style>
/* =========================
   CARD
========================= */
.card {
  background: #ffffff;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
}

/* =========================
   HEADINGS
========================= */
h2 {
  margin-bottom: 18px;
  font-size: 22px;
  font-weight: 700;
  color: #111827;
}

/* =========================
   TABLE BASE
========================= */
table {
  width: 100%;
  border-collapse: collapse;
  font-family: Arial, sans-serif;
  font-size: 14px;
  background: #ffffff;
}

/* =========================
   TABLE HEADER (ORDERS STYLE)
========================= */
thead {
  background: #e8e8e8; /* same blue as Orders page */
}

thead th {
  color: #000000;
  font-weight: 600;
  padding: 14px 16px;
  
  text-align: left;
}

/* =========================
   TABLE BODY
========================= */
tbody td {
  padding: 14px 16px;
  border-bottom: 1px solid #e5e7eb;
  color: #111827;
  vertical-align: middle;
}

/* Zebra rows */
tbody tr:nth-child(even) {
  background: #f9fafb;
}

/* Row hover */
tbody tr:hover {
  background: #f1f5f9;
}

/* =========================
   BUTTONS
========================= */
.btn-view {
  padding: 6px 14px;
  background: #1f5fd6;
  color: #ffffff;
  text-decoration: none;
  border-radius: 6px;
  font-size: 13px;
  font-weight: 600;
  transition: background 0.2s ease;
}

.btn-view:hover {
  background: #1e40af;
}

/* =========================
   PASSWORD DOTS
========================= */
.password {
  color: #6b7280;
  letter-spacing: 2px;
}

/* =========================
   ICONS
========================= */
.toggle-icon {
  margin-left: 8px;
  cursor: pointer;
  color: #2563eb;
  font-size: 15px;
}

.toggle-icon:hover {
  color: #1e40af;
}

/* =========================
   EMPTY STATE
========================= */
.no-data {
  text-align: center;
  padding: 25px;
  color: #6b7280;
  font-size: 14px;
}
</style>

</head>

<body>

<div class="card">
  <h2>👤 User List</h2>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Mobile</th>
        <th>Email</th>
        <th>Username</th>
        <th>Password</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
      <% if (users && users.length > 0) { %>
        <% users.forEach(user => { %>
          <tr>
            <td><%= user.user_id %></td>

            <td>
              <%= user.user_first_name %> <%= user.user_last_name %>
            </td>

            <td><%= user.user_mobile %></td>

            <td><%= user.user_email %></td>

            <td><%= user.user_user_name %></td>

            <!-- 🔐 PASSWORD SHOW / HIDE -->
            <td>
              <span 
                class="password"
                data-password="<%= user.user_password %>"
              >
                ••••••••
              </span>

              <i
                class="fa-solid fa-eye toggle-icon"
                title="Show / Hide Password"
                onclick="togglePassword(this)"
              ></i>
            </td>

            <td>
              <a class="btn-view" href="/admin/user/<%= user.user_id %>">
                View
              </a>
            </td>
          </tr>
        <% }) %>
      <% } else { %>
        <tr>
          <td colspan="7" class="no-data">
            No users found
          </td>
        </tr>
      <% } %>
    </tbody>
  </table>
</div>

<script>
  function togglePassword(icon) {
    const span = icon.previousElementSibling;
    const realPassword = span.getAttribute("data-password");

    if (icon.classList.contains("fa-eye")) {
      span.innerText = realPassword;
      span.style.letterSpacing = "0";
      icon.classList.remove("fa-eye");
      icon.classList.add("fa-eye-slash");
    } else {
      span.innerText = "••••••••";
      span.style.letterSpacing = "2px";
      icon.classList.remove("fa-eye-slash");
      icon.classList.add("fa-eye");
    }
  }
</script>

</body>
</html>
